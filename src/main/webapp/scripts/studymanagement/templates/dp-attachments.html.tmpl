<md-card>
  <md-card-header ng-click="$ctrl.collapsed = !$ctrl.collapsed">
    <md-card-header-text>
      <h2 class="md-title">{{'study-management.detail.attachments.table-title' | translate | fdzHyphenate}}</h2>
    </md-card-header-text>
    <md-button
      aria-label="md-card collapsed = {{$ctrl.collapsed}}}"
      class="md-card-collapsible-button"
      ng-click="$ctrl.collapsed = !$ctrl.collapsed">
      <md-icon md-font-set="material-icons" ng-if="!$ctrl.collapsed">keyboard_arrow_up</md-icon>
      <md-icon md-font-set="material-icons" ng-if="$ctrl.collapsed">keyboard_arrow_down</md-icon>
    </md-button>
  </md-card-header>
  <div ng-if="!$ctrl.collapsed">
    <md-card-content>
      <md-divider
        ng-if="$ctrl.options.study && $ctrl.options.study.length > 0"></md-divider>
      <fdz-attachment
        ng-if="$ctrl.options.study && $ctrl.options.study.length > 0"
        collapsed = true
        headline ="'search-management.filter.study'"
        lang="$ctrl.lang"
        options="$ctrl.options.study"
        template-url= "scripts/studymanagement/components/attachments.html.tmpl"
      ></fdz-attachment>
      <md-divider
        ng-if="$ctrl.options.surveys && $ctrl.options.surveys.length > 0"></md-divider>
      <fdz-attachment
        ng-if="$ctrl.options.surveys && $ctrl.options.surveys.length > 0"
        collapsed = true
        headline ="'search-management.tabs.surveys'"
        lang="$ctrl.lang"
        options="$ctrl.options.surveys"
        template-url= "scripts/studymanagement/components/attachments.html.tmpl"
      ></fdz-attachment>
      <md-divider
        ng-if="$ctrl.options.instruments && $ctrl.options.instruments.length > 0"></md-divider>
      <fdz-attachment
        ng-if="$ctrl.options.instruments && $ctrl.options.instruments.length > 0"
        collapsed = true
        headline ="'search-management.tabs.instruments'"
        lang="$ctrl.lang"
        options="$ctrl.options.instruments"
        template-url= "scripts/studymanagement/components/attachments-instrument.html.tmpl"
      ></fdz-attachment>
      <md-divider
        ng-if="$ctrl.options.dataSets && $ctrl.options.dataSets.length > 0"></md-divider>
      <fdz-attachment
        ng-if="$ctrl.options.dataSets && $ctrl.options.dataSets.length > 0"
        collapsed = true
        headline ="'search-management.tabs.data_sets'"
        lang="$ctrl.lang"
        options="$ctrl.options.dataSets"
        template-url= "scripts/studymanagement/components/attachments.html.tmpl"
      ></fdz-attachment>

    </md-card-content>
  </div>
</md-card>
