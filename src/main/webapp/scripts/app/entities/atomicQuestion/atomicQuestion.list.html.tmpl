<div>
    <div>
      <ul class="list-group">
            <li ng-repeat="atomicQuestion in currentPage._embedded.atomicQuestions" class="list-group-item">
                  <h4 class="list-group-item-heading">
                        <a ui-sref="atomicQuestion.detail({id:atomicQuestion.id})">{{atomicQuestion.id}}</a>
                  </h4>
                  <p class="list-group-item-text">{{atomicQuestion.name}}</p>
                  <p class="list-group-item-text">{{atomicQuestion.questionText[currentLanguage]}}</p>
                  <p class="list-group-item-text">{{atomicQuestion.introduction[currentLanguage]}}</p>
                  <p class="list-group-item-text">{{atomicQuestion.instruction[currentLanguage]}}</p>
                  <p class="list-group-item-text">{{atomicQuestion.sectionHeader[currentLanguage]}}</p>
                  <p class="list-group-item-text">{{atomicQuestion.type[currentLanguage]}}</p>
                  <p class="list-group-item-text">{{atomicQuestion.footnote[currentLanguage]}}</p>
                  <p class="list-group-item-text">{{atomicQuestion.compositeQuestionName}}</p>
                  <p class="list-group-item-text">  <a ui-sref="variable.detail({id:atomicQuestion.variableId})">{{atomicQuestion.variableId}}</a></p>
            </li>
      </ul>
  </div>
    <div  class="col-md-12 col-md-offset-4">
    <uib-pagination total-items="pageState.totalElements" ng-model="pageState.currentPageNumber" ng-change="pageChanged()" max-size="pageState.maxSize" class="pagination-sm" boundary-links="true" boundary-link-numbers="true" rotate="false" previous-text="{{'pager.previous' | translate}}" next-text="{{'pager.next' | translate}}" first-text="{{'pager.first' | translate}}" last-text="{{'pager.last' | translate}}"></uib-pagination>
  </div>
</div>
<div ng-if="!currentPage.$resolved">
  <loading></loading>
</div>
