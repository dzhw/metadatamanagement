(function() {
    'use strict';

    function Controller(

    ) {
      var $ctrl = this;

      $ctrl.$onInit = function() {};
    }

    angular
      .module('metadatamanagementApp')
      .controller('externalDataPackageController', Controller);
  }

)
();
