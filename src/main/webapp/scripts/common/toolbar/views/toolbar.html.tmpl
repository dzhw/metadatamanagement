<md-toolbar id="toolbar" md-whiteframe="4">
    <div ng-show="{{ENV === 'local' || ENV === 'dev' || ENV === 'test'}}" class="ribbon" ng-cloak>{{ENV}}</div>
    <div class="md-toolbar-tools" layout="row" ng-switch="isAuthenticated()">
        <md-button class="md-primary md-raised md-hue-1 md-icon-button" hide-gt-md ng-click="toggleLeft()">
            <md-icon md-font-set="material-icons">menu</md-icon>
            <md-tooltip md-z-index="10">
              {{'global.toolbar.buttons.open-menu' | translate}}
            </md-tooltip>
        </md-button>

        <div layout="row" layout-align=" center" flex class="fdz-breadCrumb">
          <div class="fdz-truncate-string" ng-repeat="item in breadCrumbItems" ng-class="{'fdz-breadCrumbLastElement':$last}">
            <a ng-if="item.id" ng-href="{{item.url[currentLanguage]}}">{{item.pageType | translate}} ({{item.id}})
              <md-tooltip md-z-index="10" ng-if="item.id">{{item.pageType | translate}} ({{item.id}})</md-tooltip>
            </a>
            <a ng-if="item.tab" ng-href="{{item.url[currentLanguage]}}">{{item.pageType | translate}} ({{item.tab | translate}})
              <md-tooltip md-z-index="10" ng-if="item.tab">{{item.pageType | translate}} ({{item.tab | translate}})</md-tooltip>
            </a>
            <a ng-if="!item.id && !item.tab" ng-href="{{item.url[currentLanguage]}}">{{item.pageType | translate}}
              <md-tooltip md-z-index="10" ng-if="!item.id && !item.tab">{{item.pageType | translate}}</md-tooltip>
            </a>
          </div>
        </div>
        <md-button
            id="changeLanguageToEn"
            aria-label="{{'global.toolbar.buttons.change-language' | translate}}"
            class="md-primary md-raised md-hue-1"
            ng-class="{'md-icon-button': $mdMedia('xs')}"
            ng-click="changeLanguage('en')"
            ng-if="currentLanguage!='en'">
            <md-tooltip md-z-index="10">
                {{'global.toolbar.buttons.change-language' | translate}}
            </md-tooltip>
            <md-icon md-font-set="material-icons">language</md-icon>
            <span class="hide-xs">English</span>
        </md-button>
        <md-button
            id="changeLanguageToDe"
            aria-label="{{'global.toolbar.buttons.change-language' | translate}}"
            class="md-primary md-raised md-hue-1"
            ng-class="{'md-icon-button': $mdMedia('xs')}"
            ng-click="changeLanguage('de')"
            ng-if="currentLanguage!='de'">
            <md-tooltip md-z-index="10">
                {{'global.toolbar.buttons.change-language' | translate}}
            </md-tooltip>
            <md-icon md-font-set="material-icons">language</md-icon>
            <span class="hide-xs">Deutsch</span>
        </md-button>
        <div ng-switch-when="true">
            <md-button id="logout" aria-label="{{'global.toolbar.buttons.logout' | translate}}" class="md-primary md-raised md-hue-1" ng-class="{'md-icon-button': $mdMedia('xs')}" ng-click="logout()">
                <md-tooltip md-z-index="10">
                    {{'global.toolbar.buttons.logout' | translate}}
                </md-tooltip>
                <md-icon md-svg-src="/assets/images/icons/logout.svg"></md-icon>
                <span class="hide-xs">{{identity.login}}</span>
            </md-button>
        </div>
        <div ng-switch-when="false">
            <md-button aria-label="{{'global.toolbar.buttons.login' | translate}}" id="login" class="md-primary md-raised md-hue-1" ng-class="{'md-icon-button': $mdMedia('xs')}" ng-click="login()">
                <md-tooltip md-z-index="10">
                    {{'global.toolbar.buttons.login' | translate}}
                </md-tooltip>
                <md-icon md-svg-src="/assets/images/icons/login.svg"></md-icon>
                <span class="hide-xs" data-translate="global.toolbar.buttons.login">Login</span>
            </md-button>
        </div>
        <div ng-switch-when="false">
            <md-button aria-label="{{'global.toolbar.buttons.register' | translate}}" id="register" class="md-primary md-raised md-hue-1" ng-class="{'md-icon-button': $mdMedia('xs')}" ng-click="register()">
                <md-tooltip md-z-index="10">
                    {{'global.toolbar.buttons.register' | translate}}
                </md-tooltip>
                <md-icon md-svg-src="/assets/images/icons/account-plus.svg"></md-icon>
                <span class="hide-xs" data-translate="global.toolbar.buttons.register">Register</span>
            </md-button>
        </div>
    </div>
</md-toolbar>
