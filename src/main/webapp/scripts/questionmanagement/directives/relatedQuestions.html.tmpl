<!--
<md-card>
    <md-card-header>
        <md-card-header-text>
            <h2 ng-if="relatedQuestionController.methodName !== 'findPredeccessors' && relatedQuestionController.methodName !== 'findSuccessors'" class="md-title">{{'question-management.detail.related-questions' | translate }}</h2>
            <h2 ng-if="relatedQuestionController.methodName === 'findPredeccessors'" class="md-title">{{'question-management.detail.predecessors' | translate }}</h2>
            <h2 ng-if="relatedQuestionController.methodName === 'findSuccessors'" class="md-title">{{'question-management.detail.successors' | translate }}</h2>
        </md-card-header-text>
    </md-card-header>
    <md-divider></md-divider>
    <div block-ui="blockRelatedQuestionCard">
      <div ng-repeat="question in relatedQuestionController.questions">
        <question-search-result search-result="question._source"></question-search-result>
      </div>
      <div ng-if="relatedQuestionController.page.totalHits > relatedQuestionController.page.size" layout="row" layout-align="center">
          <ul
              uib-pagination
              next-text="{{'global.pagination.next'|translate}}"
              previous-text="{{'global.pagination.previous'|translate}}"
              total-items="relatedQuestionController.page.totalHits"
              ng-model="relatedQuestionController.page.currentPageNumber"
              max-size="5"
              ng-change="relatedQuestionController.search()"
              items-per-page="relatedQuestionController.page.size"
              class="pagination-sm"></ul>
      </div>
      <div ng-if="relatedQuestionController.page.totalHits === 0" layout-margin>
            <span>{{'question-management.detail.no-related-questions' | translate }}</span>
      </div>
    </div>
</md-card>
-->
<div block-ui="blockRelatedQuetionContainer">
<div  ng-if="RelatedQuestionController.questions.totalHits !== 0" class="fdz-InfiniteScroll">
    <md-virtual-repeat-container id="vertical-container">
     <div md-virtual-repeat="question in RelatedQuestionController.questions" md-on-demand
         class="fdz-repeated-item" flex>
         <question-search-result search-result="question._source"></question-search-result>
     </div>
   </md-virtual-repeat-container>
</div>
  <div ng-if="RelatedQuestionController.questions.totalHits === 0" layout-margin>
    <span>{{'question-management.detail.no-related-questions' | translate }}</span>
  </div>
</div>
