/**
 * The package https manage the redirection from http pages to https pages. The use of the metadata
 * manage runs only within the https (SSL) connection.
 */
package eu.dzhw.fdz.metadatamanagement.config.https;
